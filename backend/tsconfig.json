{
  "compilerOptions": {
    /* Lenguaje y entorno */
    "target": "ES2020",                        // Usa funciones modernas (async/await, etc.)
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true,
    "module": "commonjs",                      // Node.js usa CommonJS
    "moduleResolution": "node",                // Resolución de módulos tipo Node
    "lib": ["ES2020"],                         // Librerías estándar
    "esModuleInterop": true,                   // Permite importar módulos CommonJS como si fueran ESModules
    "resolveJsonModule": true,                 // Permite importar archivos .json
    "strict": true,                            // Activa todas las comprobaciones estrictas
    "forceConsistentCasingInFileNames": true,  // Fuerza consistencia en nombres de archivos
    "skipLibCheck": true,                      // Omite revisión de tipos de dependencias (mejora tiempos de build)

    /* Entrada / salida */
    "rootDir": "src",                          // Carpeta de entrada (tu código TypeScript)
    "outDir": "dist",                          // Carpeta de salida (código compilado)

    /* Optimizaciones */
    "allowSyntheticDefaultImports": true,      // Permite importar default de módulos que no lo tienen
    "noEmitOnError": true                      // No emite si hay errores
  },
  "include": ["src"],
  "exclude": ["node_modules", "dist"]
}

